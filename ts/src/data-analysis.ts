#!/usr/bin/env node
import { run } from "./common.js";
run({"name": "ztat-ts-data-analysis", "domain": "data analysis", "purpose": "dataset profiling, metric definitions, anomaly triage, report outlines", "capabilities": ["dataset profiling, metric definitions, anomaly triage, report outlines", "validates all input as untrusted", "returns plan-only JSON by default", "requires scoped capability tokens for side effects"], "denied_actions": ["no autonomous money movement, purchasing, deletion, messaging, or network access", "no secret echoing or credential persistence", "no execution of user-provided commands"], "required_evidence": ["authenticated actor and tenant", "explicit task policy", "source provenance", "approval for regulated or irreversible actions", "audit correlation id"]});
