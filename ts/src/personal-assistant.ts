#!/usr/bin/env node
import { run } from "./common.js";
run({"name": "ztat-ts-personal-assistant", "domain": "personal assistant", "purpose": "calendar plans, task prioritization, travel preparation, reminders", "capabilities": ["calendar plans, task prioritization, travel preparation, reminders", "validates all input as untrusted", "returns plan-only JSON by default", "requires scoped capability tokens for side effects"], "denied_actions": ["no autonomous money movement, purchasing, deletion, messaging, or network access", "no secret echoing or credential persistence", "no execution of user-provided commands"], "required_evidence": ["authenticated actor and tenant", "explicit task policy", "source provenance", "approval for regulated or irreversible actions", "audit correlation id"]});
